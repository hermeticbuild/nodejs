diff --git a/BUILD.bazel b/BUILD.bazel
--- a/BUILD.bazel
+++ b/BUILD.bazel
@@ -154,6 +154,79 @@ v8_flag(name = "v8_wasm_random_fuzzers")
 
 v8_flag(name = "v8_enable_experimental_tq_to_tsa")
 
+v8_string(
+    name = "v8_target_os",
+    default = "none",
+)
+
+config_setting(
+    name = "v8_target_os_is_none",
+    flag_values = {":v8_target_os": "none"},
+)
+
+config_setting(
+    name = "v8_target_os_is_linux",
+    flag_values = {":v8_target_os": "linux"},
+)
+
+config_setting(
+    name = "v8_target_os_is_macos",
+    flag_values = {":v8_target_os": "macos"},
+)
+
+config_setting(
+    name = "v8_target_os_is_windows",
+    flag_values = {":v8_target_os": "windows"},
+)
+
+selects.config_setting_group(
+    name = "v8_target_os_default_linux",
+    match_all = [
+        ":v8_target_os_is_none",
+        "@v8//bazel/config:is_linux",
+    ],
+)
+
+selects.config_setting_group(
+    name = "v8_target_os_default_macos",
+    match_all = [
+        ":v8_target_os_is_none",
+        "@v8//bazel/config:is_macos",
+    ],
+)
+
+selects.config_setting_group(
+    name = "v8_target_os_default_windows",
+    match_all = [
+        ":v8_target_os_is_none",
+        "@v8//bazel/config:is_windows",
+    ],
+)
+
+selects.config_setting_group(
+    name = "v8_target_linux",
+    match_any = [
+        ":v8_target_os_is_linux",
+        ":v8_target_os_default_linux",
+    ],
+)
+
+selects.config_setting_group(
+    name = "v8_target_macos",
+    match_any = [
+        ":v8_target_os_is_macos",
+        ":v8_target_os_default_macos",
+    ],
+)
+
+selects.config_setting_group(
+    name = "v8_target_windows",
+    match_any = [
+        ":v8_target_os_is_windows",
+        ":v8_target_os_default_windows",
+    ],
+)
+
 # Default setting for v8_enable_maglev
 selects.config_setting_group(
     name = "maglev_by_default",
@@ -606,20 +679,24 @@ v8_config(
             "V8_HAVE_TARGET_OS",
             "V8_TARGET_OS_ANDROID",
         ],
-        "@v8//bazel/config:is_linux": [
+        ":v8_target_linux": [
             "V8_HAVE_TARGET_OS",
             "V8_TARGET_OS_LINUX",
         ],
-        "@v8//bazel/config:is_macos": [
+        ":v8_target_macos": [
             "V8_HAVE_TARGET_OS",
             "V8_TARGET_OS_MACOS",
         ],
-        "@v8//bazel/config:is_windows": [
+        ":v8_target_windows": [
             "V8_HAVE_TARGET_OS",
             "V8_TARGET_OS_WIN",
+        ],
+    }) + select({
+        "@v8//bazel/config:is_windows": [
             "UNICODE",
             "_UNICODE",
             "_CRT_RAND_S",
             "_WIN32_WINNT=0x0A00",
         ],
+        "//conditions:default": [],
     }) + select({
diff --git a/bazel/defs.bzl b/bazel/defs.bzl
--- a/bazel/defs.bzl
+++ b/bazel/defs.bzl
@@ -429,13 +429,16 @@ def _torque_files(name, noicu_srcs, icu_srcs, args, definition_extras = [], ini
         }),
     )
 
 def _v8_target_cpu_transition_impl(settings,
                                    attr, # @unused
                                   ):
-    # Check for an existing v8_target_cpu flag.
-    if "@v8//bazel/config:v8_target_cpu" in settings:
-        if settings["@v8//bazel/config:v8_target_cpu"] != "none":
-            return {}
+    result = {
+        "@v8//:v8_target_os": settings["@v8//:v8_target_os"],
+    }
+    target_cpu = settings["@v8//bazel/config:v8_target_cpu"]
+    if target_cpu != "none":
+        result["@v8//bazel/config:v8_target_cpu"] = target_cpu
+        return result
 
     # Auto-detect target architecture based on the --cpu flag.
     mapping = {
@@ -458,15 +461,22 @@ def _v8_target_cpu_transition_impl(settings,
         "riscv64": "riscv64",
         "ppc": "ppc64le",
     }
-    v8_target_cpu = mapping[settings["//command_line_option:cpu"]]
-    return {"@v8//bazel/config:v8_target_cpu": v8_target_cpu}
+    result["@v8//bazel/config:v8_target_cpu"] = mapping[settings["//command_line_option:cpu"]]
+    return result
 
-# Set the v8_target_cpu to be the correct architecture given the cpu specified
-# on the command line.
+# Preserve explicit v8_target_cpu and v8_target_os build settings when Bazel
+# applies the execution transition for mksnapshot.
 v8_target_cpu_transition = transition(
     implementation = _v8_target_cpu_transition_impl,
-    inputs = ["@v8//bazel/config:v8_target_cpu", "//command_line_option:cpu"],
-    outputs = ["@v8//bazel/config:v8_target_cpu"],
+    inputs = [
+        "@v8//:v8_target_os",
+        "@v8//bazel/config:v8_target_cpu",
+        "//command_line_option:cpu",
+    ],
+    outputs = [
+        "@v8//:v8_target_os",
+        "@v8//bazel/config:v8_target_cpu",
+    ],
 )
 
 def _mksnapshot(ctx):
